{
  "_meta": {
    "description": "道具 icon · Game Icon Institute V4_XL 专用（无 LoRA）。占位符：{{POSITIVE_G}} {{POSITIVE_L}} {{NEGATIVE}} {{WIDTH}} {{HEIGHT}} {{SEED}} {{CHECKPOINT}} {{FILENAME_PREFIX}} {{STEPS}} {{CFG}} {{SAMPLER}} {{SCHEDULER}}",
    "format": "comfyui_api",
    "category": "items",
    "recommended": {
      "width": 256,
      "height": 256,
      "gen_scale": 2.0,
      "steps": 30,
      "cfg": 6.5,
      "checkpoint": "game_icon_institute_v4_xl.safetensors",
      "lora": "",
      "notes": "触发词 gmic_(3dicon) 欧美3D icon | 现实物体+特效 | 勿叠其他 LoRA"
    }
  },
  "1": {
    "class_type": "CheckpointLoaderSimple",
    "inputs": {
      "ckpt_name": "{{CHECKPOINT}}"
    }
  },
  "2": {
    "class_type": "EmptyLatentImage",
    "inputs": {
      "width": "{{WIDTH}}",
      "height": "{{HEIGHT}}",
      "batch_size": 1
    }
  },
  "3": {
    "class_type": "CLIPTextEncodeSDXL",
    "inputs": {
      "clip": ["1", 1],
      "width": "{{WIDTH}}",
      "height": "{{HEIGHT}}",
      "crop_w": 0,
      "crop_h": 0,
      "target_width": "{{WIDTH}}",
      "target_height": "{{HEIGHT}}",
      "text_g": "{{POSITIVE_G}}",
      "text_l": "{{POSITIVE_L}}"
    }
  },
  "4": {
    "class_type": "CLIPTextEncodeSDXL",
    "inputs": {
      "clip": ["1", 1],
      "width": "{{WIDTH}}",
      "height": "{{HEIGHT}}",
      "crop_w": 0,
      "crop_h": 0,
      "target_width": "{{WIDTH}}",
      "target_height": "{{HEIGHT}}",
      "text_g": "{{NEGATIVE}}",
      "text_l": "{{NEGATIVE}}"
    }
  },
  "5": {
    "class_type": "KSampler",
    "inputs": {
      "seed": "{{SEED}}",
      "steps": "{{STEPS}}",
      "cfg": "{{CFG}}",
      "sampler_name": "{{SAMPLER}}",
      "scheduler": "{{SCHEDULER}}",
      "denoise": 1.0,
      "model": ["1", 0],
      "positive": ["3", 0],
      "negative": ["4", 0],
      "latent_image": ["2", 0]
    }
  },
  "6": {
    "class_type": "VAEDecode",
    "inputs": {
      "samples": ["5", 0],
      "vae": ["1", 2]
    }
  },
  "7": {
    "class_type": "SaveImage",
    "inputs": {
      "images": ["6", 0],
      "filename_prefix": "{{FILENAME_PREFIX}}"
    }
  }
}
