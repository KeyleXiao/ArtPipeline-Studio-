{
  "_meta": {
    "description": "道具 icon：Illustrious + RPG Item Icons LoRA(weic)。占位符：{{POSITIVE_G}} {{POSITIVE_L}} ...",
    "format": "comfyui_api",
    "category": "items",
    "recommended": {
      "width": 256,
      "height": 256,
      "gen_scale": 2.0,
      "steps": 28,
      "cfg": 5.5,
      "checkpoint": "Illustrious-XL-v2.0-stable.safetensors",
      "lora": "sdxl_rpg_item_icons.safetensors",
      "lora_strength": 0.85,
      "notes": "LoRA trigger: weic | RPG Item Icons | CFG 5.5 steps 28"
    }
  },
  "1": {
    "class_type": "CheckpointLoaderSimple",
    "inputs": {
      "ckpt_name": "{{CHECKPOINT}}"
    }
  },
  "8": {
    "class_type": "LoraLoader",
    "inputs": {
      "lora_name": "{{LORA}}",
      "strength_model": "{{LORA_STRENGTH}}",
      "strength_clip": "{{LORA_STRENGTH}}",
      "model": ["1", 0],
      "clip": ["1", 1]
    }
  },
  "2": {
    "class_type": "EmptyLatentImage",
    "inputs": {
      "width": "{{WIDTH}}",
      "height": "{{HEIGHT}}",
      "batch_size": 1
    }
  },
  "3": {
    "class_type": "CLIPTextEncodeSDXL",
    "inputs": {
      "clip": ["8", 1],
      "width": "{{WIDTH}}",
      "height": "{{HEIGHT}}",
      "crop_w": 0,
      "crop_h": 0,
      "target_width": "{{WIDTH}}",
      "target_height": "{{HEIGHT}}",
      "text_g": "{{POSITIVE_G}}",
      "text_l": "{{POSITIVE_L}}"
    }
  },
  "4": {
    "class_type": "CLIPTextEncodeSDXL",
    "inputs": {
      "clip": ["8", 1],
      "width": "{{WIDTH}}",
      "height": "{{HEIGHT}}",
      "crop_w": 0,
      "crop_h": 0,
      "target_width": "{{WIDTH}}",
      "target_height": "{{HEIGHT}}",
      "text_g": "{{NEGATIVE}}",
      "text_l": "{{NEGATIVE}}"
    }
  },
  "5": {
    "class_type": "KSampler",
    "inputs": {
      "seed": "{{SEED}}",
      "steps": "{{STEPS}}",
      "cfg": "{{CFG}}",
      "sampler_name": "{{SAMPLER}}",
      "scheduler": "{{SCHEDULER}}",
      "denoise": 1.0,
      "model": ["8", 0],
      "positive": ["3", 0],
      "negative": ["4", 0],
      "latent_image": ["2", 0]
    }
  },
  "6": {
    "class_type": "VAEDecode",
    "inputs": {
      "samples": ["5", 0],
      "vae": ["1", 2]
    }
  },
  "7": {
    "class_type": "SaveImage",
    "inputs": {
      "images": ["6", 0],
      "filename_prefix": "{{FILENAME_PREFIX}}"
    }
  }
}
